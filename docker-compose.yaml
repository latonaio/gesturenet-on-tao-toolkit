version: "3"
services:
  gesturenet-tao-toolkit:
    container_name: gesturenet-tao-toolkit
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app/src
    working_dir: /app/src
    tty: true
